<%= form_tag request.url, method: :delete, remote: :true do %>
  <fieldset>
    <legend>Are you sure you want to delete this record?</legend>
    <ul>
      <li>
        <%= label_tag :current_user, "User swipe card id/barcode" %> 
        <%= password_field_tag :current_user %>
      </li>
    </ul>
    <%= submit_tag "Delete" %>
  </fieldset>
<% end %>